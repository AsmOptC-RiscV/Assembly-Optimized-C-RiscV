PREFIX=${PREFIX:-/opt/riscv/bin/riscv64-unknown-elf}
AR=${AR:-${PREFIX}-ar}
GCC=${GCC:-${PREFIX}-gcc}
CLANG=clang
GDB=${GDB:-${PREFIX}-gdb}
OBJDUMP=${OBJDUMP:-${PREFIX}-objdump}
OPENOCD=${OPENOCD:-/opt/riscv/bin/openocd}
ECHO=${ECHO:-echo}
